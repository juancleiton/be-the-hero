import React from 'react';
import ReactDOM from 'react-dom'; //Serve para manipular a DOM
import App from './App'; //Este é um componente 

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
